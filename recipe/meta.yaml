{% set name = "pandamesh" %}
{% set version = "0.1.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pandamesh-{{ version }}.tar.gz
  sha256: 68dce880df92d92cc9edfe15d4bc76064a4e24f296a2c219f3970ed7f6fb25a9

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.8
    - setuptools >=64.0.0
    - wheel
    - pip
  run:
    - python >=3.8
    - geopandas
    - py-triangle
    - gmsh
    - python-gmsh
    - pooch
    - shapely >=2.0

test:
  imports:
    - pandamesh
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/deltares/pandamesh
  summary: From geodataframe to mesh
  license: MIT
  license_family: MIT
  license_file: LICENSE
  doc_url: https://deltares.github.io/pandamesh/
  dev_url: https://github.com/deltares/pandamesh

extra:
  recipe-maintainers:
    - Huite
    - JoerivanEngelen
    - visr
